/**
 * Welcome! Your task is to modify the 
 * function's name to startCleaningProcess, 
 * and make sure you also adjust the 
 * function call in the main method accordingly. 
 * This change will give you hands-on experience 
 * with renaming and invoking functions in Scala. 
 * Enjoy the practice!
 */

// TODO: Rename this function to startCleaningProcess
// Define a function to perform a task for a robot
def startCleaningProcess(): Unit =
    println("Cleaning mode activated!")

@main def run: Unit =
    // TODO: Rename the function call below to match the new function name
    // Call the function to activate the robot's cleaning mode
    startCleaningProcess()
