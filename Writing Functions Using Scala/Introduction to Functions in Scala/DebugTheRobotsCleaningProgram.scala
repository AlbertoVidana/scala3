/**
 * Your home robot is ready to start its 
 * cleaning tasks, but there's a tiny bug in 
 * its program that's preventing it from 
 * doing so. Let's debug the Scala code 
 * together and fix this issue so that 
 * the robot can get to work tidying up the 
 * room!
 */

 def cleanRoom(): Unit = 
  println("The room is now clean!")

@main def run: Unit = 
  cleanRoom()